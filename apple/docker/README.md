# apple / flink

For this image build you need the flink distribution being available on a local web server as explained in the main readme in the repo root.
The difference is that this image is based on the apple internal jdk base image. 
